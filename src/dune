(executables
 (names
  run_soc
  run_soc_noises
  gather_arm
  opt_multiscale
  run_baseline
  run_arm
  run_fast
  costs
  run_soft_soc
  run_log_barrier
  diff_c
  qtuu
  run_soft_relu)
 (libraries accessor adam owl owl-ode cmdargs owl-opt owl-opt-lbfgs arm lib)
 (preprocess
  (pps ppx-owl-opt)))
