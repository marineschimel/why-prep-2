(executables
 (names
  run_soc
  run_soc_noises_mp
  run_sparse_soc
  run_log_barrier
  run_soc_noises
  costs
  run_soft_soc)
 (libraries owl owl-ode cmdargs owl-opt owl-opt-lbfgs arm lib)
 (preprocess
  (pps ppx-owl-opt)))
